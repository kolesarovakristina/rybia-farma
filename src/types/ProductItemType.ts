export type TProductItemProps = {
  readonly productId: string;
  readonly icon: string;
  readonly price: number;
  readonly name: string;
  readonly fishAmout: number;
  readonly quantity?: number;
};
